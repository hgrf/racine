version: "3.9"
services:
  web:
    build:
      context: ..
      dockerfile: .ci/Dockerfile
    ports:
      - "8080:80"
    image: holgergraef/msm
  test:
    build:
      context: ..
      dockerfile: .ci/Dockerfile
    depends_on:
      - web
    command: python -m pytest
